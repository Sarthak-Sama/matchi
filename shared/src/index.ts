/**
 * Scaffolding placeholder for the @tokyo/shared package.
 *
 * This module exists so the workspace wiring (package exports, TypeScript
 * project references, build pipeline) can be verified end to end before any
 * domain logic (Zod contracts, scoring constants, etc.) is added by later
 * tasks.
 */
export const SHARED_PACKAGE_NAME = "@tokyo/shared" as const;
